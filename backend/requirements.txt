# Pinned to the exact versions installed locally, where the PDF test harness
# (test_themes.py) runs — dev and production must resolve identically.
# Floating ranges let a Space rebuild pull a new minor overnight and break
# production silently (fpdf2 2.8 already deprecated `output(dest=...)`
# under the old `>=2.7.0` pin). Bump these deliberately, never implicitly,
# and re-run test_themes.py after any bump.
#
# Verified 2026-08-28 on Python 3.11 (Dockerfile target: 3.12 — all pins
# support both).
fastapi==0.137.2
uvicorn[standard]==0.49.0
pydantic==2.13.4
# google-generativeai is end-of-life — replaced by google-genai.
google-genai==1.39.1
google-api-python-client==2.197.0
fpdf2==2.8.7
qrcode==8.2
Pillow==12.3.0
slowapi==0.1.10
